# Benchmark v0

Placeholder. Populate `manifest.json` once the first labeled samples land.

Target composition:
- **Plate**: 200 images spanning the 10 most common dishes we expect in-app.
- **Pour**: 30 videos per liquid type covering the volume ranges in
  `docs/COLLECTION.md`.

Until real data is collected, `evaluate --bench v0` will refuse to run
rather than produce a meaningless scorecard.
